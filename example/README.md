# pdfme_flutter example

Demonstrates on-device PDF generation from a pdfme Designer template.

```bash
flutter pub get
flutter run
```

Tap **Generate PDF**, then open the saved `output.pdf` path shown in the UI.
